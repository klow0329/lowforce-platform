{
  "name": "lowforce-platform",
  "version": "0.1.0",
  "description": "LowForce Event/Exhibition Management Platform — Phase 1",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node src/server.js"
  }
}
